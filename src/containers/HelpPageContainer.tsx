import {connect} from 'react-redux';
import HelpPage from '../components/HelpPage';

const mapStateToProps = () => ({});

const mapActionCreators = {};

export default connect(mapStateToProps, mapActionCreators)(HelpPage);
