import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  text-align: center;
`;

const LoadingInfo = () => (
  <Wrapper>Bewegungen werden geladen ...</Wrapper>
);

export default LoadingInfo;
